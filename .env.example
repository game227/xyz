# Django
SECRET_KEY=change-me-generate-a-real-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL (yagona baza — SQLite/MongoDB yo‘q)
DB_NAME=xyz
DB_USER=xyz_user
DB_PASSWORD=your-password
DB_HOST=localhost
DB_PORT=5432

# Timezone
TIME_ZONE=Asia/Tashkent
